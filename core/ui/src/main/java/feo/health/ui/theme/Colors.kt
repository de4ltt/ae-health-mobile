package feo.health.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Light gray color token.
 */
val LIGHT_GRAY = Color(232, 237, 245, 255)

/**
 * Off-white background color token.
 */
val WHITE = Color(247, 250, 252, 255)

/**
 * Dark charcoal black background color token.
 */
val BLACK = Color(13, 20, 28, 255)

/**
 * Muted secondary dizzy blue color token.
 */
val DIZZY_BLUE = Color(74, 115, 156, 255)

/**
 * Primary accent blue color token.
 */
val BLUE = Color(23, 133, 242, 255)

/**
 * Standard green success status color token.
 */
val GREEN = Color(2, 136, 0, 255)

/**
 * Standard red error status color token.
 */
val RED = Color(145, 0, 0, 255)

/**
 * Standard orange warning status color token.
 */
val ORANGE = Color(176, 120, 0, 255)

/**
 * Fully transparent clear color token.
 */
val TRANSPARENT = Color(0,0,0,0)