package feo.health.ui.theme

import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.ui.unit.dp

/**
 * Padding layout tokens used to align components inside containers consistently.
 */
object HPadding {
    /**
     * Standard common padding values applied for buttons, dialogs, and text inputs.
     */
    val common = PaddingValues(
        horizontal = 15.dp,
        vertical = 12.dp
    )
}